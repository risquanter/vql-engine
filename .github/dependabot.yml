version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current: Dependabot opens a
  # review-and-approve PR with the new commit SHA when an action releases.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
